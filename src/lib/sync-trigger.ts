/**
 * sync-trigger — đã vô hiệu hoá (không còn dùng Google Sheets sync)
 * Giữ file để các API routes vẫn import được mà không lỗi.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function triggerSheetSync(_target: string): void {
  // no-op — Google Sheets sync đã bị gỡ bỏ
}
